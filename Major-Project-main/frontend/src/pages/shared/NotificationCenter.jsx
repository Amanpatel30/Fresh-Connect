import React from 'react';
import { Box, Typography } from '@mui/material';

const NotificationCenter = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h1">Notification Center</Typography>
    </Box>
  );
};

export default NotificationCenter; 